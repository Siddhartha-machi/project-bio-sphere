enum ButtonVariant { primary, secondary, error }

enum ButtonType { filled, outlined, text }

/// ------------- Tab enums -------------- ///
enum OpenMode { view, editOrCreate }

/// ------------- Charts -------------- ///

enum ChartType { bar, pie, line, scatter }

enum LegendOrientation { horizontal, vertical }

enum ChartUnits { none, percentage, currency }
